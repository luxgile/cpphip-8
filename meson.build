project('cpphip8', 'cpp',
  version : '0.1',
  default_options : ['warning_level=3',
                     'cpp_std=c++23'])

exe = executable('cpphip8', 'main.cpp',
  install : true)

test('basic', exe)
